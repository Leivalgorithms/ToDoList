name: mini_notion
description: "Una aplicación estilo Notion simplificada para organizar notas, tareas e ideas"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  dcm: ^2.1.0

flutter:
  uses-material-design: true
